# assets

ゲーム本体で使う画像等のアセットを配置するディレクトリ。

## 構成

- `characters/`: キャラクター画像
- `icons/`: アイコン
- `backgrounds/`: 背景画像

## 現在の状態

`game/`の実装が未着手のため、本バージョンではアセットは未追加。教材ごとの画像（レッスン内で使う図解等）は、ここではなく各教材の`curriculum/<theme>/<ID>/images/`に置く。
